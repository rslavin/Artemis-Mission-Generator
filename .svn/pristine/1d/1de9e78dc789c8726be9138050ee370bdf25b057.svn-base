package mission.event.commands;

import mission.event.Command;

public class CModProperty extends Command {

	protected CModProperty(String tag) {
		super(tag);
	}
	
	public void init(Object name, String property, Object value) {
		put("name", name);
		put("property", property);
		put("value", value);
	}
	
}
