package mission.event.conditions;

import mission.event.Condition;
import mission.expression.Expression;

public class CDifficulty extends Condition {

	public CDifficulty(String cmp, Expression val) {
		super("if_difficulty");
		
		put("comparator", cmp);
		put("value", val);
	}

}
